# Case 03

Request: prep for a public opponent whose open results suggest one shell but whose latest event result may indicate a pivot.

Checks:

- handles conflicting public clues explicitly
- avoids false certainty
- keeps the prep advice robust to more than one likely shell
