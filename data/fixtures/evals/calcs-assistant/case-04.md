# Case 04

Request: use a fully specified Pelipper into Charizard rain calc and explain whether the survival benchmark actually changes the spread decision.

Checks:

- uses the exact-browser path or clearly indicates why it could not
- reports the backend-backed exact result only if verification succeeded
- translates the result into a spread or preserve decision
