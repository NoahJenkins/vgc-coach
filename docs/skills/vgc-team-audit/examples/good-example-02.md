# Good Example 2

Request:

`This team is weird on purpose. Tell me what is actually broken versus what just looks unconventional.`

Why this is good:

- forces the audit to distinguish structural weakness from stylistic difference
- rewards identity-preserving fixes instead of generic normalization
- keeps the weird parts that still support the plan
- makes the smallest useful changes first instead of jumping straight to a rebuild
