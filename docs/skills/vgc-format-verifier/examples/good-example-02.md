# Good Example 2

Request:

`I think Regulation M-A is still active this week. Verify that and tell me whether my legality assumptions are safe.`

Why this is good:

- uses absolute dates and the active window
- treats the answer as a format-state check, not a meta summary
- makes any remaining uncertainty explicit instead of bluffing
