# Good Example 2

Request:

`Help me reason through whether this Sneasler speed benchmark matters if I already have Tailwind support.`

Why this is good:

- treats the calc as a speed-decision problem, not just raw math
- compares the benchmark against real game support
- translates the result into team-building value instead of isolated numbers

# Good Example 2B

Request:

`The builder is choosing between two Sneasler spreads. Does the key speed benchmark still matter if the final team already has Tailwind and Fake Out support?`

Why this is good:

- keeps the handoff narrow
- frames speed as a build tradeoff instead of fake exact math
- gives the builder a clear yes or no consequence for the final spread
