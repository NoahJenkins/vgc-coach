---
description: Create matchup-specific lead plans and preserve targets
---

Use the `vgc-lead-planner` skill for this request.

Treat `AGENTS.md` as the shared repo contract and `docs/runtime/opencode.md` as the OpenCode adapter.

User request:
$ARGUMENTS
