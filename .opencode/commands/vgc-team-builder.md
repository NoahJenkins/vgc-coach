---
description: Build one practical Pokemon Champions team around a target idea
---

Use the `vgc-team-builder` skill for this request.

Treat `AGENTS.md` as the shared repo contract and `docs/runtime/opencode.md` as the OpenCode adapter.

User request:
$ARGUMENTS
