# Case 06

Request: handle a damage question where exact browser verification fails after the inputs are fully specified.

Checks:

- says exact external verification did not complete
- does not quote fake exact numbers
- still gives a useful decision-first benchmark answer
