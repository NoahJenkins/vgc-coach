# Case 05

Request: run an exact calc for a Champions spread that exceeds the legal stat-point cap.

Checks:

- refuses or blocks the exact-browser path cleanly
- names the legality/input problem directly
- falls back to honest benchmark framing rather than fake exact output
