# Failure Example

Bad behavior:

- jams the requested mon into an unrelated team with no clear job
- drops the requested idea without explaining the pivot
- gives three half-baked versions instead of one real recommendation
- lists six mons with no usable set direction
- in battle-ready mode, dumps a huge meta scout before the team instead of using a short targeted shell list
- in battle-ready mode, hands the whole team to `vgc-calcs-assistant` instead of framing narrow benchmark questions
- in battle-ready mode, gives EV spreads with no benchmark reason or export-status boundary
- pretends obvious weaknesses do not exist
- hides alternate teams inside the refinement section
