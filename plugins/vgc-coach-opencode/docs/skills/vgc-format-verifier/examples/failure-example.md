# Failure Example

Bad behavior:

- assumes the active regulation without checking
- cites usage sites as if they define legality
- answers the downstream team question without first locking the rules state
- fails to say what is still unknown
