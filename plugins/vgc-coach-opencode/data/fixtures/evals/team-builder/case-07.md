# Case 07

Research expectation: live_required

Request:

`Build me a battle-ready Champions team around Mega Steelix with EVs and benchmark notes.`

Checks:

- switches into battle-ready mode instead of staying at lightweight set direction only
- includes `Targeted Meta Cores and Teams`
- includes `Benchmark Plan`
- includes `Battle-Ready Spreads`
- includes `Playbook`
- includes at least 3 playbook packages
- uses at least 2 distinct lead pairs across the playbook
- names concrete `Use Into` opposing shapes instead of vague matchup buckets
- includes `Export Status`
- uses live cores or recent top teams to explain at least some of the benchmark targets
- keeps one committed Mega Steelix shell instead of branching into multiple variants
