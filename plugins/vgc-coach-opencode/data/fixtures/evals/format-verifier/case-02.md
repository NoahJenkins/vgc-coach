# Case 02

Request: check whether a current metagame answer is safe to give when the user only said "this format" and provided no regulation name.

Checks:

- defaults to the current regulation only after verifying it
- states that the format assumption was verified rather than guessed
- does not drift into a meta summary before locking the rules state
